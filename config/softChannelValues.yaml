# softChannelValues.yaml — persisted runtime values for software channels.
# This file is auto-written by the control node on every Set() call.
#
# Values here override the 'default' in softChannels.yaml on startup.
# Safe to edit manually ONLY when the control node is not running.
# Format: values: { <refDes>: <float>, ... }
values: {}
