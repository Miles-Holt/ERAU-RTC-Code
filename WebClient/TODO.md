# WebClient TODO

## Open

- [x] **Update channel roles in app.js** — change `role === 'cmd'` checks to handle `cmd-bool`, `cmd-pct`, `cmd-float`; drive widget type from channel role instead of control subType
- [ ] **Redesign UI tiles** — update card layouts to match desired appearance for each control type (valve, sensor, ignition, bang-bang, etc.)
- [ ] **Update UI colors** — revise color scheme in `css/style.css`
- [ ] **UI layout editor** — create a drag-and-drop or config-driven feature to let the user arrange cards on the front panel

## Done

<!-- Completed items go here -->